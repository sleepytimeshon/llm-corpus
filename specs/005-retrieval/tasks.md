---
description: "Task list for feature 005-retrieval (SP-005)"
---

# Tasks: Hybrid Retrieval — BM25 + Dense + Graph + Confidence Fusion, Cancellable Search, Atomic Index Updates

**Input**: Design documents from `/specs/005-retrieval/`
**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/{search-hit-schema.json, error-envelope-schema.json, adr-embedding-model.md, adr-rrf-fusion.md, adr-edges-materialization.md}

**Prior state**: SP-001 + SP-002 + SP-003 + SP-004 merged on `main`. SP-001 registered the `corpus.find` MCP tool with a placeholder handler returning `{hits: [], query}` (per `packages/transport/src/corpus-find-tool.ts` comment "Real implementation lands in SP-005") and an `export {};` `packages/index/src/index.ts` stub. SP-002 supplies read-only resources + `parseMarkdownWithFrontmatter` codec. SP-003 supplies sentinel rows + drain-lock + ingest pipeline. SP-004 supplies classified `documents` rows + body-file frontmatter mirror + proposed-state taxonomy. SP-005 builds *additively*: grows `packages/index/` from stub to functional; extends `packages/pipeline/`, `packages/storage/`, `packages/contracts/`, `packages/inference/`; replaces the SP-001 `corpus-find-tool.ts` handler body; adds `corpus reindex` CLI; extends daemon post-classify hook chain.

**Tests**: MANDATORY for tasks touching IO, retrieval output, telemetry, paths, schema validation, atomic writes, or Constitution Principles I/V/VII/VIII/IX/X/XI/XII/XIII/XIV/XVI. SP-005 touches all of these — tests are mandatory throughout. Phases 2A/2B are RED-phase (failing tests authored first); Phases 3A/3B are GREEN-phase (implementations turn them green) per plan.md two-phase build.

**Scope-bound**: SP-005 ships ONLY the retrieval owner — embedding adapter + FTS5/vec/graph/confidence retrievers + RRF fusion + search orchestrator + edges builder + index persister + schema migration + `corpus.find` real handler + `corpus reindex` CLI + daemon hook chain extension + PREREQ-001..PREREQ-009 forward-compatibility plumbing. SP-005 does NOT ship Tier 1/2/3 fallthrough (SP-006), kill-9 cross-stage recovery (SP-006), `corpus://failures` MCP resource (SP-006), retrieval-eval harness (v1.5+), chunked embeddings (future), worker-pool parallelism (future), non-Ollama embedding adapters (future), MCP mutation surfaces (FORBIDDEN by Principle III).

**Organization**: Tasks grouped by phase per the SP-003/SP-004 tasks.md convention. Phase 1 = setup/prereq verification; Phase 2 = foundational PREREQs; Phase 3 = US1 P1 MVP (autonomous indexing + ranked `corpus.find`); Phase 4 = US2 P1 (`corpus reindex` backfill); Phase 5 = US3 P2 (filter narrowing); Phase 6 = lint + constitution enforcement; Phase 7 = polish/verification. Constitution Check 16/16 [x] verified at plan time; Complexity Tracking empty.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: US1, US2, or US3 (maps to user stories in spec.md). Phase 3-5 user-story tasks carry this label; setup/foundational/polish/lint tasks do not.
- File paths are repo-relative under `~/Projects/llm-corpus/`
- Trailing markup: `*Constitution X, FR-RETRIEVAL-Y, SC-RETRIEVAL-Z*` references

## Path Conventions

Repo-relative paths under `~/Projects/llm-corpus/`. SP-005 grows one previously-stub package (`packages/index/`), extends four (`packages/pipeline/`, `packages/storage/`, `packages/contracts/`, `packages/inference/`), replaces one tool handler body (`packages/transport/src/corpus-find-tool.ts`), and adds one CLI subcommand (`packages/cli/`) + one daemon hook extension (`packages/daemon/`).

**Branch note**: Branch `005-retrieval` already checked out; spec/plan/data-model/contracts/research files at this path are the proof. No T-stub task required.

---

## Phase 1: Setup

**Purpose**: Single one-shot prerequisite check before any code work. SP-005's runtime depends on a reachable Ollama with `nomic-embed-text` loaded AND the sqlite-vec native addon loadable.

- [ ] T001 Verify SP-005 runtime prerequisites — assert `curl -fsS http://localhost:11434/api/version` returns JSON containing `"version"`; assert `curl -fsS http://localhost:11434/api/tags | jq -e '.models[].name | select(. == "nomic-embed-text")'` returns non-empty (pull via `ollama pull nomic-embed-text` if absent); assert `node -e "const v = require('sqlite-vec'); const Database = require('better-sqlite3'); const db = new Database(':memory:'); v.load(db); console.log(db.prepare('SELECT vec_distance_cosine(?,?) AS d').get(Buffer.alloc(12), Buffer.alloc(12)))"` exits 0 (namespace load + `vec_distance_cosine` function registered); document Ollama version + model digest + sqlite-vec version in `specs/005-retrieval/quickstart.md` "Operator prereqs". *Constitution I, Decision A, Decision B, Assumption "Ollama is installed and running locally with nomic-embed-text available"*

**Checkpoint**: Phase 1 ends here. Ollama reachable with `nomic-embed-text`; sqlite-vec loads via namespace import; `vec_distance_cosine` SQL function callable.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: PREREQ-001..PREREQ-009 from plan.md. Each PREREQ gets a TDD contract-test/implementation pair (or a single assertion task for stub verification). Forward-compatibility plumbing; not principle violations.

**⚠️ CRITICAL**: No user-story work can begin until this phase is complete — Phase 3+ source compiles against the contracts shipped here.

### Tests-First (mandatory per Constitution V/VII/VIII/IX/XIII/XIV)

- [ ] T002 [P] Unit test `tests/unit/search-input-schema.test.ts` — assert `SearchInputZodSchema.parse({query: 'x'})` succeeds with default `limit=20`; rejects `query` longer than 2048 chars; rejects unknown filter keys (strict mode → `validation_error`); rejects `limit < 1` or `limit > 100`; rejects malformed `since`/`until` (non-ISO-8601); rejects unknown top-level keys. *PREREQ-001, FR-RETRIEVAL-001, FR-RETRIEVAL-020, Constitution V*
- [ ] T003 [P] Unit test `tests/unit/search-hit-schema.test.ts` — assert `SearchHitZodSchema.parse(<valid>)` succeeds with `{uri: 'corpus://docs/doc-deadbeef', score: 1.23, title: 't', facet_domain: 'd', facet_type: 'tutorial', tags: ['a'], snippet: 's'}`; rejects malformed `uri` (must match `^corpus://docs/doc-[0-9a-f]{8}$`); rejects `facet_type` not in `FACET_TYPE_VALUES`; rejects `snippet` > 400 chars; rejects unknown keys (strict). *PREREQ-001, FR-RETRIEVAL-001, FR-RETRIEVAL-020, SC-RETRIEVAL-020, Constitution V*
- [ ] T004 [P] Unit test `tests/unit/error-envelope-schema.test.ts` — assert `SearchErrorEnvelopeZodSchema.parse({error_code: 'validation_error', message: 'm', hint: 'h'})` succeeds; rejects unknown `error_code`; rejects `message` or `hint` > 1024 chars; assert closed enum `SEARCH_ERROR_CODES = ['validation_error','embedding_unavailable','index_unavailable','query_aborted','all_signals_failed','internal_error']`. *PREREQ-001, FR-RETRIEVAL-004, Constitution V*
- [ ] T005 [P] Unit test `tests/unit/telemetry-sp005-classes.test.ts` — Zod round-trip for all 14 new SP-005 event classes (`embed.started`, `embed.completed`, `embed.failed`; `index.started`, `index.completed`, `index.failed`; `edges.started`, `edges.completed`, `edges.failed`; `search.started`, `search.query`, `search.completed`, `search.degraded`, `search.error`); each validates envelope (`event`, `timestamp` ISO-8601, `severity`, `outcome`) + class-specific fields per data-model.md §"Entity 5"; per-class ≤ 4096-byte serialization assertion (Constitution IX); existing SP-001..SP-004 union variants still parse unchanged. `search.query` schema asserts `query_hash: /^[0-9a-f]{64}$/`, `tier_used: 'hybrid'`, `result_count: int>=0`, `signals_used: array of enum`. *PREREQ-002, FR-RETRIEVAL-013, FR-RETRIEVAL-023, Constitution I, V, IX, XIII*
- [ ] T006 [P] Unit test `tests/unit/errors-sp005.test.ts` — 9 new typed errors (`RetrievalError` base, `EmbeddingUnavailableError`, `EmbeddingDimensionMismatchError`, `IndexUnavailableError`, `EdgesBuildTimeoutError`, `SearchAbortedError`, `SearchValidationError`, `FusionError`, `IndexPersistError`) instantiate with structured `data`, throwable, distinct `name`, all `instanceof RetrievalError` (except subclasses derived from `RetrievalError`). Zero `process.exit` references in source. *PREREQ-003, FR-RETRIEVAL-014, Constitution XI*
- [ ] T007 [P] Unit test `tests/unit/policies-sp005-fields.test.ts` — `interactivePolicy` and `batchPolicy` (extended) validate against the extended `PolicySchema` carrying new fields `perDocEmbedTimeoutMs`, `perDocIndexTimeoutMs`, `perDocEdgesBuildTimeoutMs`, `embeddingHttpTimeoutMs`, `retrieverSqlTimeoutMs`, `searchTotalTimeoutMs`, `topKPerRetriever`. Defaults match PREREQ-004: interactive `{10_000, 5_000, 15_000, 10_000, 5_000, 30_000, 64}`, batch `{30_000, 10_000, 60_000, 30_000, 10_000, 60_000, 64}`. Existing SP-003/SP-004 fields parse unchanged. *PREREQ-004, FR-RETRIEVAL-011, FR-RETRIEVAL-022, Decision L, Constitution VI, VII*
- [ ] T008 [P] Unit test `tests/unit/sp005-migration.test.ts` — `runSp005Migration(db)` on empty DB creates `documents_fts` (FTS5 with `tokenize='porter unicode61'`), `documents_vec` (vec0 with `float[768]`), `edges` (with CHECK on `kind`, PRIMARY KEY composite, two indexes). Second invocation on same DB produces zero side effects (idempotent — `IF NOT EXISTS` clauses); existing SP-002/SP-003/SP-004 tables unchanged; runtime <50ms on idempotent re-run. *PREREQ-005, FR-RETRIEVAL-019, SC-RETRIEVAL-022, Constitution X*
- [ ] T009 [P] Unit test `tests/unit/native-addon-allowlist-sqlite-vec.test.ts` — assert `build/verify-native-addons.ts` allowlist contains `sqlite-vec`; assert `import * as sqliteVec from 'sqlite-vec'; sqliteVec.load(db)` works on a fresh `better-sqlite3` connection; assert `vec_distance_cosine` function is registered after load. *PREREQ-005, Decision B, Risk R1*
- [ ] T010 [P] Unit test `tests/unit/config-loader-retrieval-sections.test.ts` — `loadConfig()` parses `[embedding]` (`model` default `'nomic-embed-text'`, `endpoint` default `'http://localhost:11434/api/embeddings'`), `[ranker]` (`rrf_k=60`, `tag_overlap_threshold=0.3`, `summary_similarity_threshold=0.7`, `recency_boost_days=90`, `recency_boost_weight=0.05`, `schema_bump_penalty_weight=-0.10`), `[ranker.confidence_weights]` (§10.5 defaults: `research-paper=1.20, manual=1.10, form=1.10, reference=1.10, article=1.00, notes=0.95, transcript=0.90, podcast=0.90, video=0.90, book=1.05`), `[search]` (`default_top_k=20`, `min_results=0`). Unknown facet_type weight defaults to `1.0`. *PREREQ-004, FR-RETRIEVAL-009, Decision G, Decision M*
- [ ] T011 [P] Unit test `tests/unit/index-stub-replaced.test.ts` — assert `packages/index/src/index.ts` exports the new SP-005 surface (`searchOrchestrator`, `fuseRrf`, `Fts5Adapter`, `VecAdapter`, `GraphAdapter`, `ConfidenceAdapter`, `buildEdges`); the pre-SP-005 `export {};` stub no longer compiles as a valid file content match. *PREREQ-008*
- [ ] T012 [P] Unit test `tests/unit/corpus-find-tool-handler-replaced.test.ts` — assert `packages/transport/src/corpus-find-tool.ts` no longer returns the SP-001 placeholder `{hits: [], query}`; instead its body delegates to `packages/index/src/search.ts` `searchOrchestrator`; tool name + input/output Zod schema surface (registered by SP-001) preserved unchanged. *PREREQ-009, FR-RETRIEVAL-017, Constitution III*

### Implementation

- [ ] T013 [P] Implement `packages/contracts/src/search-schemas.ts` — exports `SearchInputZodSchema` (strict, `query` max 2048 chars, `filters` optional closed-vocab, `limit` int [1,100] default 20), `SearchFiltersZodSchema` (strict: `facet_domain?`, `facet_type?: string|string[]`, `tags?: string[]`, `since?: ISO8601`, `until?: ISO8601`, `source_type?`), `SearchHitZodSchema` (strict, `uri` regex `^corpus://docs/doc-[0-9a-f]{8}$`, `facet_type: z.enum(FACET_TYPE_VALUES)`, `snippet` max 400), `SearchErrorEnvelopeZodSchema` (strict, closed `error_code` enum, `message`/`hint` max 1024), `SearchOutputZodSchema` (union of success-shape and envelope-shape). Re-exported from `packages/contracts/index.ts`. *PREREQ-001, FR-RETRIEVAL-001, FR-RETRIEVAL-004, FR-RETRIEVAL-020, Constitution V*
- [ ] T014 [P] Extend `packages/contracts/src/telemetry.ts` — add 14 new SP-005 event class Zod schemas to the `TelemetryEvent` discriminated union per data-model.md §"Entity 5". `search.query` schema includes `query_hash: z.string().regex(/^[0-9a-f]{64}$/)`, `tier_used: z.literal('hybrid')`, `result_count: z.number().int().min(0)`, `signals_used: z.array(z.enum(['bm25','dense','graph','confidence']))`. Each schema asserts ≤4096-byte serialization. Pre-existing variants compile unchanged. Re-export from index. *PREREQ-002, FR-RETRIEVAL-013, FR-RETRIEVAL-023, SC-RETRIEVAL-010, SC-RETRIEVAL-016, Constitution I, V, IX, XIII*
- [ ] T015 [P] Extend `packages/contracts/src/errors.ts` — add 9 typed errors (`RetrievalError` base + 8 subclasses). Each subclass of `Error` (or `RetrievalError`) with stable `name`, structured `data`, zero `process.exit`. Re-export from index. *PREREQ-003, FR-RETRIEVAL-014, Constitution XI*
- [ ] T016 [P] Extend `packages/pipeline/src/policies.ts` — add SP-005 fields to `PolicySchema` AND extend `interactivePolicy` / `batchPolicy` with defaults per PREREQ-004. Re-export unchanged. *PREREQ-004, FR-RETRIEVAL-011, FR-RETRIEVAL-022, Decision L, Constitution VI, VII*
- [ ] T017 [P] Implement `packages/storage/src/sp005-migration.ts` — `runSp005Migration(db)` executes the three `CREATE VIRTUAL/TABLE IF NOT EXISTS` + two `CREATE INDEX IF NOT EXISTS` from FR-RETRIEVAL-019 verbatim. Wired into `runSchemaMigration(db)` after SP-004's migration. Called against a `Database` connection that has already had `sqliteVec.load(db)` invoked. *PREREQ-005, FR-RETRIEVAL-019, SC-RETRIEVAL-022, Constitution X*
- [ ] T018 Extend `packages/storage/src/index.ts` — `openIndexReadWrite()` now calls `sqliteVec.load(db)` at connection-open time (after `better-sqlite3` open, before `runSchemaMigration`); export `loadSqliteVec(db)` helper. Existing SP-001..SP-004 callers unaffected. *PREREQ-005, Decision B*
- [ ] T019 [P] Implement `packages/storage/src/index-persister.ts` — `persistIndex({docId, ftsFields, vector, edges, signal}, db): Promise<Result<void, IndexPersistError>>` executes within a CALLER-OPENED transaction (the orchestrator opens `BEGIN IMMEDIATE`): `INSERT INTO documents_fts(doc_id, title, summary, tags, facet_topic, body_excerpt) VALUES (?,?,?,?,?,?)` then `INSERT INTO documents_vec(doc_id, embedding) VALUES (?, ?)` (vector serialized as `Float32Array` buffer) then for each edge `INSERT INTO edges(src_id, dst_id, kind, weight) VALUES (?,?,?,?) ON CONFLICT(src_id,dst_id,kind) DO NOTHING`. Returns success/err; never commits/rolls back (caller owns transaction). `signal.throwIfAborted()` between rows. *PREREQ-006, FR-RETRIEVAL-007, Constitution VII, VIII*
- [ ] T020 [P] Implement `packages/inference/src/embedding-adapter.ts` — `class EmbeddingAdapter` with constructor `({model, endpoint, expectedDim})`; methods `embedDocument(text, signal)` and `embedQuery(text, signal)` returning `Promise<Result<Float32Array, EmbeddingError>>`. Issues `undici.fetch(endpoint, {method: 'POST', body: JSON.stringify({model, prompt: text}), signal, headers: {'Content-Type': 'application/json'}})` (Ollama legacy single-prompt `/api/embeddings`); on `ECONNREFUSED` returns `Result.err(EmbeddingUnavailableError({errno}))`; parses `body.embedding: number[]`, asserts `length === expectedDim` else `EmbeddingDimensionMismatchError`, converts to `Float32Array`, asserts all entries finite. Emits `embed.started` / `embed.completed` / `embed.failed` telemetry. Routes through SP-001 egress hook (localhost allowlisted). *PREREQ-007, FR-RETRIEVAL-005, FR-RETRIEVAL-006, FR-RETRIEVAL-013, Decision D, Constitution I, V, VII, XIII*
- [ ] T021 [P] Update `eslint.config.js` to scope existing custom rules (`no-process-exit-in-libs`, `paths-from-resolver-only`, `no-shell-string-exec`, `no-promise-race-settimeout`, `no-forbidden-network-imports`) over the new SP-005 source paths: `packages/index/src/**`, `packages/inference/src/embedding-adapter.ts`, `packages/pipeline/src/{embed,index,edges-build,retrieval-orchestrator}-stage.ts`, `packages/storage/src/{index-persister,sp005-migration}.ts`, `packages/contracts/src/search-schemas.ts`, `packages/transport/src/corpus-find-tool.ts`, `packages/cli/src/reindex-command.ts`. *SC-RETRIEVAL-017, SC-RETRIEVAL-018, SC-RETRIEVAL-019, Constitution VII, XI, XII, XIV*
- [ ] T022 [P] Add fixture inputs to `tests/fixtures/sp005-retrieval/` — `seeded-classified-corpus.sql` (20 classified docs across mixed facet_types + tags for fusion fixtures), `mock-embedding-response.json` (768-dim vector deterministic case), `mock-embedding-bad-dim.json` (wrong-dimension failure injection), `disable-signal-fixture.ts` (per-signal harness override toggling one retriever off), `README.md` documenting provenance. *plan.md Project Structure, SC-RETRIEVAL-003, SC-RETRIEVAL-004*

**Checkpoint**: `npm run build` succeeds; `npm run test:unit` passes for Phase 2 PREREQ tests; `npm run lint` exits 0; `runSp005Migration(db)` on a fresh DB produces the three SP-005 tables; SP-005 contract surface exists in `@llm-corpus/contracts` and `@llm-corpus/storage` and `@llm-corpus/inference`. Forward-compat plumbing ready; user-story implementation can begin.

---

## Phase 3: User Story 1 — Autonomous Indexing on Ingest + Ranked `corpus.find` (Priority: P1) 🎯 MVP

**Goal**: SP-003 daemon's post-classify hook chain auto-invokes embed → index → edges-build sub-stages after each classify commit; within the per-document four-sub-stage budget the row gains FTS5 + vec + zero-or-more edges entries; `corpus.find` invoked from any MCP client returns a ranked SearchHit list fusing all four signals via RRF (k=60) + confidence multipliers.

**Independent Test**: With Ollama + nomic-embed-text loaded and the SP-003 daemon running (post-SP-005 hook chain), drop one PDF + one Markdown + one plain-text + one HTML article into the inbox; within the per-doc four-sub-stage budget assert: 4 `documents_fts` rows + 4 `documents_vec` rows + zero-or-more `edges` rows. Then invoke `corpus.find` with a natural-language query and assert a ranked SearchHit list with `signals_used: ['bm25','dense','graph','confidence']`, `tier_used: 'hybrid'`.

### Tests for User Story 1 (RED phase — Constitution VIII TDD imperative — Phase 2B per plan.md)

> **NOTE: Write these tests FIRST, ensure they FAIL before implementation.**

- [ ] T023 [P] [US1] Unit test `tests/unit/embedding-adapter.test.ts` — `EmbeddingAdapter.embedDocument('text', signal)` posts to `http://localhost:11434/api/embeddings` via `undici.fetch` with body `{model, prompt}`; propagates AbortSignal end-to-end (mid-flight `controller.abort()` aborts the fetch; no orphan socket); on `ECONNREFUSED` returns `Result.err(EmbeddingUnavailableError({errno: 'ECONNREFUSED'}))`; on wrong-dimension response returns `Result.err(EmbeddingDimensionMismatchError({expected: 768, got: 1024}))`; emits `embed.started`/`embed.completed`/`embed.failed` telemetry. *FR-RETRIEVAL-005, FR-RETRIEVAL-006, FR-RETRIEVAL-011, FR-RETRIEVAL-013, Decision D, Constitution I, V, VII, XIII*
- [ ] T024 [P] [US1] Unit test `tests/unit/fts5-adapter.test.ts` — `Fts5Adapter.search(query, {topK, filters, signal})` renders a field-weighted BM25 query string (`summary:5, tags:3, facet_topic:2, title:2, body_excerpt:1` — Decision C verbatim §10.1); returns `Result<RankingSignal, IndexUnavailableError>` with `{kind: 'bm25', results: [{doc_id, rank, score}], succeeded: true}`; corrupt-FTS5 case (DROP TABLE injected) returns `Result.err(IndexUnavailableError)`; supports `WHERE` filter pushdown for `facet_domain`, `facet_type IN (...)`, `tags`, `since`/`until`, `source_type`. Module path: `packages/index/src/fts5-adapter.ts`. *FR-RETRIEVAL-002, FR-RETRIEVAL-021, Decision C, Constitution V, VII*
- [ ] T025 [P] [US1] Unit test `tests/unit/vec-adapter.test.ts` — `VecAdapter.search(queryEmbedding, {topK, filters, signal})` runs `SELECT doc_id, vec_distance_cosine(embedding, ?) AS dist FROM documents_vec ORDER BY dist LIMIT ?`; returns `RankingSignal {kind: 'dense', ...}`; filter pushdown via JOIN on `documents`; embedding adapter unreachable at query time → `Result.err(EmbeddingUnavailableError)` (the dense retriever embeds the QUERY at query-time per FR-RETRIEVAL-005). Module path: `packages/index/src/vec-adapter.ts`. *FR-RETRIEVAL-002, FR-RETRIEVAL-005, Decision B, Constitution V, VII*
- [ ] T026 [P] [US1] Unit test `tests/unit/graph-adapter.test.ts` — `GraphAdapter.search(seedDocIds, {topK, signal})` runs `SELECT dst_id, kind, weight FROM edges WHERE src_id IN (?) UNION SELECT src_id, kind, weight FROM edges WHERE dst_id IN (?) ORDER BY weight DESC LIMIT ?` (bidirectional traversal per FR-RETRIEVAL-008); returns `RankingSignal {kind: 'graph', ...}`; empty seeds → empty results (no error); filters narrow via JOIN on `documents`. Module path: `packages/index/src/graph-adapter.ts`. *FR-RETRIEVAL-002, FR-RETRIEVAL-008, Decision E, Constitution V, VII*
- [ ] T027 [P] [US1] Unit test `tests/unit/confidence-adapter.test.ts` — `ConfidenceAdapter.score(docIds, {weightsConfig, signal})` looks up each doc's `facet_type` and `ingest_timestamp`; applies `confidence_weight[facet_type]` (default 1.0 for unknown) × recency adjustment (`+0.05` if within 90 days, `-0.10` if older than schema_version bump). Returns `RankingSignal {kind: 'confidence', results: [{doc_id, rank, score}]}` ordered descending by adjusted weight. Module path: `packages/index/src/confidence-adapter.ts`. *FR-RETRIEVAL-002, FR-RETRIEVAL-009, Decision G, Constitution V, VII*
- [ ] T028 [P] [US1] Unit test `tests/unit/fusion.test.ts` — `fuseRrf(signals: RankingSignal[], {k: 60, confidenceWeights}): SearchHit[]` implements RRF per ARCHITECTURE-FINAL §10.3 verbatim: `score(doc) = sum over r of 1/(k + rank_r(doc))`; confidence multiplier applied AFTER fusion (`final = rrf × confidence_weight × recency`); covers all retriever-result combinations (none-empty, one-empty, two-empty, three-empty, four-empty — R7); deterministic ordering on fixture input. Module path: `packages/index/src/fusion.ts`. *FR-RETRIEVAL-002, FR-RETRIEVAL-009, SC-RETRIEVAL-005, Decision G, R7, Constitution V*
- [ ] T029 [P] [US1] Unit test `tests/unit/edges-builder.test.ts` — `buildEdges({newDocId, newDocTags, newDocEmbedding, newDocFrontmatterRelated, db, thresholds, signal}): EdgeRecord[]` computes (a) Jaccard tag-overlap vs every existing classified doc — emit edge if ≥0.3 (configurable); (b) cosine similarity vs every existing `documents_vec.embedding` via `vec_distance_cosine` — emit edge if ≥0.7 (configurable); (c) for every entry in `frontmatter.related`, emit `(newDocId, entry, 'explicit_related', 1.0)` unconditionally. Directional: only `(D_new, D_i)` inserts (queries traverse both directions). Cancellable via signal. Module path: `packages/index/src/edges-builder.ts`. *FR-RETRIEVAL-008, Decision E, Constitution V, VII*
- [ ] T030 [P] [US1] Unit test `tests/unit/index-persister-atomicity.test.ts` — `persistIndex` writes documents_fts + documents_vec + edges rows within a CALLER-OPENED transaction; injection of SQL exception between vec INSERT and edges INSERT triggers caller's ROLLBACK → assert: zero rows in any of three tables for the failed doc; `index.failed` event emitted with `error_code='persist_failed'`. *FR-RETRIEVAL-007, SC-RETRIEVAL-011, Constitution VIII*
- [ ] T031 [P] [US1] Unit test `tests/unit/search-orchestrator.test.ts` — `searchOrchestrator({input: SearchInput, db, embeddingAdapter, weightsConfig, policy, signal, disabledSignals?}): Promise<SearchOutput>` runs four retrievers via `Promise.allSettled` with per-retriever AbortControllers chained off the master signal; collects RankingSignal[] including failures; runs `fuseRrf`; validates output via `SearchHitZodSchema.array().parse`; on validation failure returns `{error_code: 'internal_error', ...}` envelope. Emits `search.started`/`search.query`/`search.completed`/`search.degraded`/`search.error` telemetry. *FR-RETRIEVAL-001, FR-RETRIEVAL-002, FR-RETRIEVAL-003, FR-RETRIEVAL-013, FR-RETRIEVAL-020, FR-RETRIEVAL-023, SC-RETRIEVAL-020, Constitution V, VII, XIII*
- [ ] T032 [P] [US1] Unit test `tests/unit/search-degraded-signals.test.ts` — mid-query mock of embedding adapter returning `EmbeddingUnavailableError` — assert: response shape is success with `hits` non-empty (from BM25 + graph + confidence) AND `degraded_signals: ['dense']` AND `signals_used: ['bm25','graph','confidence']` AND `search.degraded` event emitted. NOT a `search.error`. (FR-003 acceptance scenario explicit.) *FR-RETRIEVAL-002, FR-RETRIEVAL-004, SC-RETRIEVAL-004, Constitution XVI*
- [ ] T033 [P] [US1] Unit test `tests/unit/embed-stage.test.ts` — `embedStage({docId, db, embeddingAdapter, signal}): Promise<Result<Float32Array, EmbedStageError>>` loads doc via `fetchDocument`, reads body file via `parseMarkdownWithFrontmatter`, concatenates `(title + summary + facet_topic + tags.join(' ') + body_excerpt[:500_words])`, posts to embedding adapter, returns vector. Per-doc AbortController with `setTimeout(() => controller.abort('per_doc_embed_timeout'), policy.perDocEmbedTimeoutMs)` + `clearTimeout` on success (NEVER `Promise.race(setTimeout)`). Module path: `packages/pipeline/src/embed-stage.ts`. *FR-RETRIEVAL-005, FR-RETRIEVAL-006, FR-RETRIEVAL-011, FR-RETRIEVAL-021, Constitution VII*
- [ ] T034 [P] [US1] Unit test `tests/unit/index-stage.test.ts` — `indexStage({docId, embedding, db, signal}): Promise<Result<void, IndexStageError>>` reads `documents` row, reads body file frontmatter via `parseMarkdownWithFrontmatter` (extract `summary`, `facet_topic`), builds `ftsFields = {title, summary, tags: csvJoin, facet_topic, body_excerpt: first500Words}`, calls `persistIndex` within caller-opened transaction. Codepoint-safe body excerpt truncation. Module path: `packages/pipeline/src/index-stage.ts`. *FR-RETRIEVAL-005, FR-RETRIEVAL-007, FR-RETRIEVAL-021, Constitution VII, VIII*
- [ ] T035 [P] [US1] Unit test `tests/unit/edges-build-stage.test.ts` — `edgesBuildStage({newDocId, newDocEmbedding, db, thresholds, signal}): Promise<Result<EdgeRecord[], EdgesBuildTimeoutError>>` calls `buildEdges` with per-doc AbortController + `setTimeout` per `policy.perDocEdgesBuildTimeoutMs`; on timeout returns `Result.err(EdgesBuildTimeoutError)`; on success returns the edges array (caller persists). Module path: `packages/pipeline/src/edges-build-stage.ts`. *FR-RETRIEVAL-008, FR-RETRIEVAL-011, Edge Case "Edges-builder timeout"*
- [ ] T036 [P] [US1] Unit test `tests/unit/retrieval-orchestrator.test.ts` — `retrievalOrchestrator({docId, db, embeddingAdapter, policy, signal}): Promise<Result<void, RetrievalError>>` wraps the post-classify hook chain: opens `BEGIN IMMEDIATE`, calls SP-004 `classify-persister` UPDATE + taxonomy INSERTs, calls `embedStage` (BEFORE transaction technically — embedding runs OUTSIDE the SQL transaction; ONLY the SQL writes are inside per R6), then back inside the transaction calls `indexStage` + `edgesBuildStage` + `persistIndex`, COMMITs, then SP-004 post-COMMIT body-file rename. On any failure between BEGIN and COMMIT: ROLLBACK + sidecar at `Paths.failed()`. Module path: `packages/pipeline/src/retrieval-orchestrator.ts`. *FR-RETRIEVAL-006, FR-RETRIEVAL-007, FR-RETRIEVAL-008, FR-RETRIEVAL-018, SC-RETRIEVAL-011, SC-RETRIEVAL-012, R6, Constitution VIII, IX*
- [ ] T037 [P] [US1] Integration test `tests/integration/end-to-end-retrieval.test.ts` — boot SP-003 daemon with `batchPolicy` (post-SP-005 hook chain); drop one PDF + one Markdown + one plain-text + one HTML article into `Paths.inbox()`; within per-doc four-sub-stage budget (30s interactive / 120s batch) assert: 4 `documents_fts` rows + 4 `documents_vec` rows + zero-or-more `edges` rows. Then invoke `corpus.find({query: <natural-language>})` via MCP-test-client; assert response is `{hits: SearchHit[], query, result_count, tier_used: 'hybrid', signals_used: ['bm25','dense','graph','confidence']}`; `hits[0].uri` resolvable via SP-002 read path; each hit Zod-validates. Live Ollama + nomic-embed-text required. *FR-RETRIEVAL-001, FR-RETRIEVAL-002, FR-RETRIEVAL-006, FR-RETRIEVAL-007, FR-RETRIEVAL-021, SC-RETRIEVAL-001, SC-RETRIEVAL-002, US1 Acceptance 1+2+5, Constitution XVI*
- [ ] T038 [P] [US1] Integration test `tests/integration/retrieval-atomicity.test.ts` — inject a SQL exception between `INSERT documents_vec` and `INSERT edges` (mock-better-sqlite3 wrapper); assert: transaction rolls back AND zero rows in any of three SP-005 tables for the failed doc AND row `documents.facet_type` reverts to `'unclassified'` (SP-004 UPDATE was in same transaction per FR-RETRIEVAL-007) AND tmp body file removed from `Paths.cache()` AND `index.failed` event emitted with `error_code='persist_failed'`. *FR-RETRIEVAL-007, SC-RETRIEVAL-011, SC-RETRIEVAL-012, US1 robustness, Constitution VIII*
- [ ] T039 [P] [US1] Integration test `tests/integration/retrieval-embedding-unavailable.test.ts` — stop the local Ollama process (or point adapter at unused port 11499); drop a file into the inbox; observe `embed.failed` (severity error, `errno: 'ECONNREFUSED'`) + `<doc-id>.error.json` sidecar at `Paths.failed()` with `error_code='embedding_unavailable', retriable=true, stage='embed'`; daemon does NOT crash; row stays classified-but-unindexed. *FR-RETRIEVAL-006, FR-RETRIEVAL-013, Edge Case "Embedding model offline at query time"*
- [ ] T040 [P] [US1] Integration test `tests/integration/retrieval-degraded-signals.test.ts` — with indexed corpus, unload `nomic-embed-text` from Ollama mid-test; invoke `corpus.find({query})`; assert: successful response (NOT transport error) with `degraded_signals: ['dense']`, `signals_used: ['bm25','graph','confidence']`, `hits` non-empty, `search.degraded` event fires. Repeat for FTS5-corrupted (drop the table) → `degraded_signals: ['bm25']`. *FR-RETRIEVAL-002, FR-RETRIEVAL-004, SC-RETRIEVAL-004, Edge Case "FTS5 table corrupted/missing", Constitution XVI*
- [ ] T041 [P] [US1] Integration test `tests/integration/retrieval-query-aborted.test.ts` — start a `corpus.find` call with caller-supplied AbortController; abort mid-flight; assert: response is `{error_code: 'query_aborted', ...}` envelope (NOT transport error) within 2s of abort; all four retrievers' AbortControllers fire; process does NOT exit. *FR-RETRIEVAL-004, FR-RETRIEVAL-011, SC-RETRIEVAL-014, Constitution VII*

### Implementation for User Story 1 (GREEN phase — Phase 3B per plan.md)

- [ ] T042 [P] [US1] Implement `packages/index/src/fts5-adapter.ts` — `class Fts5Adapter` with `search(query, {topK, filters, signal})` rendering field-weighted FTS5 MATCH query (`summary:5 OR tags:3 OR facet_topic:2 OR title:2 OR body_excerpt:1` per §10.1 verbatim); prepared statement against `documents_fts` JOINed on `documents` for filter pushdown; codepoint-safe query escaping; `signal.throwIfAborted()` between rows. *FR-RETRIEVAL-002, FR-RETRIEVAL-021, Decision C*
- [ ] T043 [P] [US1] Implement `packages/index/src/vec-adapter.ts` — `class VecAdapter` with `search(queryEmbedding, {topK, filters, signal})` running `SELECT doc_id, vec_distance_cosine(embedding, ?) AS dist FROM documents_vec [JOIN documents ON doc_id=id WHERE ...] ORDER BY dist LIMIT ?`; embedding adapter handles query-time embedding. *FR-RETRIEVAL-002, FR-RETRIEVAL-005, Decision B*
- [ ] T044 [P] [US1] Implement `packages/index/src/graph-adapter.ts` — `class GraphAdapter` with `search(seedDocIds, {topK, filters, signal})` running bidirectional `UNION` traversal over `edges`. Seeds are typically the union of FTS5 + vec top-K candidate doc_ids. *FR-RETRIEVAL-002, FR-RETRIEVAL-008*
- [ ] T045 [P] [US1] Implement `packages/index/src/confidence-adapter.ts` — `class ConfidenceAdapter` with `score(docIds, {weightsConfig, signal})` looking up `facet_type` + `ingest_timestamp` and computing the adjusted weight per FR-RETRIEVAL-009. *FR-RETRIEVAL-002, FR-RETRIEVAL-009, Decision G*
- [ ] T046 [P] [US1] Implement `packages/index/src/edges-builder.ts` — `buildEdges(...)` per FR-RETRIEVAL-008 verbatim; thresholds from config (defaults Jaccard 0.3, cosine 0.7). *FR-RETRIEVAL-008, Decision E*
- [ ] T047 [P] [US1] Implement `packages/index/src/fusion.ts` — `fuseRrf(signals, {k=60, confidenceWeights}): SearchHit[]`; covers all retriever-empty combinations per R7. *FR-RETRIEVAL-002, FR-RETRIEVAL-009, Decision G, R7*
- [ ] T048 [US1] Implement `packages/index/src/search.ts` — `searchOrchestrator({input, db, embeddingAdapter, weightsConfig, policy, signal, disabledSignals?}): Promise<SearchOutput>` runs the four retrievers via `Promise.allSettled` with per-retriever AbortControllers chained off `signal`; per-call timeouts via `AbortController + setTimeout + clearTimeout` (NEVER `Promise.race(setTimeout)`); SHA-256-hashes the raw query for `search.query` telemetry; runs `fuseRrf`; validates output; returns SearchOutput. Annotates `degraded_signals` for partial failures; returns envelope for total failures. *FR-RETRIEVAL-001, FR-RETRIEVAL-002, FR-RETRIEVAL-003, FR-RETRIEVAL-004, FR-RETRIEVAL-011, FR-RETRIEVAL-013, FR-RETRIEVAL-020, FR-RETRIEVAL-023, SC-RETRIEVAL-014, SC-RETRIEVAL-016, Constitution I, V, VII, XIII, XVI*
- [ ] T049 [US1] Implement `packages/index/src/index.ts` — replaces the SP-001-era `export {};` stub. Re-exports `searchOrchestrator`, `fuseRrf`, `Fts5Adapter`, `VecAdapter`, `GraphAdapter`, `ConfidenceAdapter`, `buildEdges`. *PREREQ-008, plan.md Project Structure*
- [ ] T050 [P] [US1] Implement `packages/pipeline/src/embed-stage.ts` — `embedStage({docId, db, embeddingAdapter, policy, signal})` per FR-RETRIEVAL-006 verbatim with per-doc AbortController timeout. *FR-RETRIEVAL-005, FR-RETRIEVAL-006, FR-RETRIEVAL-011, FR-RETRIEVAL-021*
- [ ] T051 [P] [US1] Implement `packages/pipeline/src/index-stage.ts` — `indexStage({docId, embedding, db, signal})` reads body file frontmatter, builds ftsFields, calls `persistIndex` within caller-opened transaction. *FR-RETRIEVAL-007, FR-RETRIEVAL-021, Constitution VIII*
- [ ] T052 [P] [US1] Implement `packages/pipeline/src/edges-build-stage.ts` — `edgesBuildStage({newDocId, newDocEmbedding, db, thresholds, policy, signal})` calls `buildEdges` with per-doc AbortController timeout. *FR-RETRIEVAL-008, FR-RETRIEVAL-011*
- [ ] T053 [US1] Implement `packages/pipeline/src/retrieval-orchestrator.ts` — wraps the four-sub-stage chain: embedStage (OUTSIDE SQL tx per R6) → `BEGIN IMMEDIATE` → SP-004 classify-persister UPDATE + taxonomy INSERTs → indexStage + edgesBuildStage + persistIndex → COMMIT → SP-004 post-COMMIT body-file rename. ROLLBACK + sidecar on any failure between BEGIN and COMMIT. *FR-RETRIEVAL-006, FR-RETRIEVAL-007, FR-RETRIEVAL-008, FR-RETRIEVAL-018, R6, Constitution VIII, IX*
- [ ] T054 [US1] Extend `packages/storage/src/classify-persister.ts` — SP-004's persister no longer commits its own transaction; transaction lifetime moved to `retrieval-orchestrator.ts` per plan.md. Keep the same write semantics; just don't `BEGIN`/`COMMIT` (caller owns). Adds new orchestrator-controlled signature `persistClassificationInTransaction({...}, db, signal)`. *FR-RETRIEVAL-007, Constitution VIII*
- [ ] T055 [US1] REPLACE `packages/transport/src/corpus-find-tool.ts` — the SP-001 placeholder handler is replaced. Tool registration (name, input/output Zod schema surface) preserved verbatim. New handler validates input via `SearchInputZodSchema.parse` → on `ZodError` returns `{error_code: 'validation_error', ...}` envelope as successful MCP response (NOT transport error). On success delegates to `searchOrchestrator`; wraps `SearchOutput` into MCP tool response. Caller AbortSignal propagated end-to-end. *PREREQ-009, FR-RETRIEVAL-001, FR-RETRIEVAL-004, FR-RETRIEVAL-011, FR-RETRIEVAL-017, FR-RETRIEVAL-020, SC-RETRIEVAL-009, Constitution III, V, VII*
- [ ] T056 [US1] Extend `packages/daemon/src/index.ts` — post-classify hook chain extends to invoke `retrievalOrchestrator` after each successful classify. Master `AbortController` propagates SIGTERM to all four sub-stages. The drain-lock acquired at drain start is HELD continuously across ingest → classify → embed → index → edges-build per FR-RETRIEVAL-018. *FR-RETRIEVAL-006, FR-RETRIEVAL-018, SC-RETRIEVAL-013, Constitution VII, IX*

**Checkpoint US1**: live-Ollama end-to-end indexing + ranked `corpus.find` works against a fresh corpus on pai-node01; tests T037, T038, T040, T041 pass; classify-stage tests still pass (no regression); Phase A+B gate from plan.md cleared.

---

## Phase 4: User Story 2 — `corpus reindex` CLI Backfill (Priority: P1)

**Goal**: User runs `corpus reindex` to backfill `documents_fts` + `documents_vec` + `edges` for classified-but-unindexed rows; CLI reuses `Paths.drainLock()`; concurrent invocations emit `pipeline.lock_contention` and exit 0; `--dry-run` mode lists candidates without Ollama calls or SQL writes.

**Independent Test**: Start with corpus containing 30 classified rows (post-SP-004 state) where the three SP-005 tables are EMPTY; run `corpus reindex`; observe stderr progress + summary line; `SELECT COUNT(*) FROM documents_vec` ≥ 30 post-run; `corpus.find` returns ranked results.

### Tests for User Story 2 (RED phase)

- [ ] T057 [P] [US2] Unit test `tests/unit/reindex-command.test.ts` — `reindexCommand({dryRun, signal, policy})` acquires `Paths.drainLock()` via `withDrainLock`; selects rows `WHERE facet_type != 'unclassified' AND status='success' AND NOT EXISTS (SELECT 1 FROM documents_vec WHERE doc_id = documents.id)`; iterates and calls `embedStage` + `indexStage` + `edgesBuildStage`; emits progress under `interactivePolicy`; exits 0 with summary line `indexed=N, failed=M, skipped=K`. Module path: `packages/cli/src/reindex-command.ts`. *FR-RETRIEVAL-012, FR-RETRIEVAL-018, SC-RETRIEVAL-006, US2 Acceptance 1*
- [ ] T058 [P] [US2] Unit test `tests/unit/reindex-dry-run.test.ts` — `reindexCommand({dryRun: true, ...})` lists candidate rows AND issues ZERO Ollama HTTP calls AND issues ZERO SQL writes to the three SP-005 tables AND exits 0. *FR-RETRIEVAL-012, US2 Acceptance 4*
- [ ] T059 [P] [US2] Unit test `tests/unit/reindex-idempotency.test.ts` — running `reindexCommand` on a fully-indexed corpus produces `indexed=0, failed=0, skipped=N`; SQL idempotency check `WHERE NOT EXISTS (SELECT 1 FROM documents_vec WHERE doc_id = ?)` short-circuits; zero Ollama HTTP calls. Completes in <5s on a 100-doc corpus. *FR-RETRIEVAL-012, SC-RETRIEVAL-015, US2 Acceptance 3, Constitution X*
- [ ] T060 [P] [US2] Integration test `tests/integration/reindex-cli.test.ts` — seed a corpus with 30 classified rows with EMPTY three SP-005 tables; run `corpus reindex`; assert: exit 0 + stderr progress lines + summary `indexed=30, failed=0, skipped=0` + post-run row counts. Live Ollama + nomic-embed-text required. *FR-RETRIEVAL-012, SC-RETRIEVAL-006, US2 Acceptance 1*
- [ ] T061 [P] [US2] Integration test `tests/integration/reindex-lock-contention.test.ts` — start SP-003 daemon holding `Paths.drainLock()`; invoke `corpus reindex` concurrently; assert: emits `pipeline.lock_contention` telemetry event AND exits 0 within 100ms AND issues ZERO Ollama HTTP calls. *FR-RETRIEVAL-018, SC-RETRIEVAL-007, SC-RETRIEVAL-013, US2 Acceptance 2*
- [ ] T062 [P] [US2] Integration test `tests/integration/reindex-sigterm.test.ts` — start `corpus reindex` against 100 rows; send SIGTERM mid-stream; assert: in-flight sub-stage aborted via `controller.abort()`; in-flight row's partial index state rolls back per Constitution VIII; command exits within 2s with non-zero status; `pipeline.aborted` event emitted with `stage='reindex'`. *FR-RETRIEVAL-011, US2 Acceptance 5, Constitution VII*

### Implementation for User Story 2

- [ ] T063 [US2] Implement `packages/cli/src/reindex-command.ts` — `corpus reindex [--dry-run]` command; acquires `Paths.drainLock()` via `withDrainLock`; on contention emits `pipeline.lock_contention` and exits 0; iterates classified-but-unindexed rows; invokes `embedStage` + `indexStage` + `edgesBuildStage` per row (single transaction per row); emits per-doc progress under `interactivePolicy`; final summary line to stdout. SIGTERM-aware (master controller). `process.exit` permitted (CLI boundary per Constitution XI). *FR-RETRIEVAL-012, FR-RETRIEVAL-014, FR-RETRIEVAL-018, SC-RETRIEVAL-006, SC-RETRIEVAL-007, SC-RETRIEVAL-013, SC-RETRIEVAL-015, Constitution VII, IX, XI*
- [ ] T064 [US2] Register `reindex-command` in `packages/cli/src/index.ts` — wire the subcommand into the existing CLI entrypoint per SP-003/SP-004 convention. *FR-RETRIEVAL-012*

**Checkpoint US2**: `corpus reindex` backfills classified-but-unindexed rows; lock contention behavior matches SP-003/SP-004; dry-run mode does what it says; SIGTERM aborts cleanly.

---

## Phase 5: User Story 3 — Filters Narrow `corpus.find` Results (Priority: P2)

**Goal**: `corpus.find` accepts structured filter facets (`facet_domain`, `facet_type`, `tags`, `since`, `until`, `source_type`); each retriever pushes filters into its SQL `WHERE` clause BEFORE fusion; unknown filter keys produce `validation_error` envelope.

**Independent Test**: With indexed corpus, invoke `corpus.find({query, filters: {facet_type: ['tutorial']}})`; assert every hit has `facet_type='tutorial'`. Invoke with `{filters: {since: '2026-04-01'}}`; assert every hit's `ingest_timestamp >= '2026-04-01'`. Invoke with `{filters: {zzz_unknown: 'foo'}}`; assert `validation_error` envelope returned as successful MCP response.

### Tests for User Story 3 (RED phase)

- [ ] T065 [P] [US3] Unit test `tests/unit/filter-pushdown.test.ts` — each retriever's SQL gains `AND documents.facet_domain = ?` / `AND documents.facet_type IN (?,?)` / `AND documents.ingest_timestamp >= ?` clauses when filters set; unfiltered when filters empty; filter set propagated to all four retrievers (no retriever silently ignores filters). *FR-RETRIEVAL-001, SC-RETRIEVAL-008, US3 Acceptance 1+2+3+5*
- [ ] T066 [P] [US3] Unit test `tests/unit/filter-validation-error.test.ts` — `corpus.find({query, filters: {zzz_unknown: 'foo'}})` → `SearchInputZodSchema.parse` fails (strict mode) → handler returns `{error_code: 'validation_error', message, hint}` envelope as successful MCP response (NOT transport error); `search.error` event emitted with `error_code='validation_error'`. *FR-RETRIEVAL-004, FR-RETRIEVAL-020, SC-RETRIEVAL-009, US3 Acceptance 4*
- [ ] T067 [P] [US3] Integration test `tests/integration/retrieval-filter-pushdown.test.ts` — with seeded corpus of mixed `facet_type` documents, invoke `corpus.find` with each filter facet combination; assert filter narrowing AND `filters_applied` annotation in response. Edge case: empty filter object `filters: {}` produces unfiltered ranked list (no SQL filter clauses). *FR-RETRIEVAL-001, SC-RETRIEVAL-008, US3 Acceptance 1+2+3+5*

### Implementation for User Story 3

- [ ] T068 [US3] Extend `packages/index/src/{fts5,vec,graph,confidence}-adapter.ts` — each retriever's SQL `WHERE` clause gains filter pushdown (parameterized) when `SearchFilters` is non-empty. JOIN on `documents` table where needed (FTS5 already references `doc_id`). Adds `filters_applied` annotation to the fusion output. *FR-RETRIEVAL-001, SC-RETRIEVAL-008*
- [ ] T069 [US3] Extend `packages/index/src/search.ts` — propagate `input.filters` to all four retrievers; annotate response with `filters_applied: input.filters`; empty filter object produces unfiltered query. *FR-RETRIEVAL-001, SC-RETRIEVAL-008, US3 Acceptance 5*

**Checkpoint US3**: filter pushdown narrows the candidate set before fusion; unknown filter keys produce envelope responses; empty filter object is unfiltered.

---

## Phase 6: Lint + Constitution Enforcement

**Purpose**: Cross-cutting lints + four-signal acceptance assertions (FR-RETRIEVAL-003 ACS#1-4) + telemetry budget enforcement.

- [ ] T070 [P] Extend ESLint rules to cover `packages/index/` — `no-process-exit-in-libs`, `paths-from-resolver-only`, `no-shell-string-exec`, `no-promise-race-settimeout`, `no-forbidden-network-imports` all scope over `packages/index/src/**` per Phase 2 T021. Run `npm run lint`; exit 0. *SC-RETRIEVAL-017, SC-RETRIEVAL-018, SC-RETRIEVAL-019, Constitution VII, XI, XII, XIV*
- [ ] T071 [P] Constitution VIII grep-lint `tests/lint/no-index-writes-outside-transaction.test.ts` — assert every call site of `persistIndex` AND every direct `INSERT INTO documents_fts/_vec/edges` in SP-005 source is INSIDE a `BEGIN`/`COMMIT` block (orchestrator-controlled). Run via repo-wide AST scan of `packages/{index,pipeline,storage,transport,cli,daemon}/src/**`. *FR-RETRIEVAL-007, SC-RETRIEVAL-011, SC-RETRIEVAL-012, Constitution VIII*
- [ ] T072 [P] Telemetry-budget lint `tests/lint/telemetry-size-budget.test.ts` — every SP-005 telemetry event class serialized at max-field-width validates ≤4096 bytes per Constitution IX. Run against fixture inputs covering each of the 14 event classes. *FR-RETRIEVAL-013, Constitution IX*
- [ ] T073 [P] Telemetry body-content-prohibition lint `tests/lint/telemetry-no-body-content.test.ts` — across every test run, zero telemetry event payloads contain substrings drawn from body text of indexed documents NOR raw query text; `search.query` events carry `query_hash` (SHA-256 hex) only. Verified by injecting body-content-as-query and asserting hash-only telemetry. *FR-RETRIEVAL-023, SC-RETRIEVAL-016, Constitution I*
- [ ] T074 [P] Four-signal-acceptance test `tests/integration/four-signals-must-all-be-inputs.test.ts` — ACS#1: representative query executes all four signals (assert telemetry includes BM25 + dense + graph + confidence signal entries). *FR-RETRIEVAL-002, FR-RETRIEVAL-003, US1 Acceptance 3*
- [ ] T075 [P] Signal-degradation-test `tests/integration/four-signals-survive-failure.test.ts` — ACS#2: kill embedding adapter mid-query; assert: `corpus.find` still returns non-empty hits + `degraded_signals: ['dense']` + `search.degraded` event (NOT `search.error`). *FR-RETRIEVAL-002, FR-RETRIEVAL-003, FR-RETRIEVAL-004, SC-RETRIEVAL-004*
- [ ] T076 [P] Single-signal-match test `tests/integration/four-signals-single-match.test.ts` — ACS#3: literal-rare-term query matches only BM25; assert: response `result_count >= 1` with BM25-matching doc at top rank; other three signals' contributions are zero but response is success-shaped. *FR-RETRIEVAL-002, FR-RETRIEVAL-003, SC-RETRIEVAL-005, Edge Case "Single-signal match"*
- [ ] T077 [P] Signal-disable-measurable-change test `tests/integration/four-signals-disable-changes-topk.test.ts` — ACS#4: with seeded ≥20-doc corpus, run `corpus.find` five times — once with all signals + four times each with one signal disabled via `disabledSignals` test-harness override — assert: each disabled-signal run produces top-K materially different from the all-on run (≥1 swap in top-K rank-order OR ≥1 doc appears in one but not the other). *FR-RETRIEVAL-003, SC-RETRIEVAL-003, R10*
- [ ] T078 [P] Drain-lock concurrency test `tests/integration/retrieval-concurrency.test.ts` — launch `corpus drain` + `corpus reenrich` + `corpus reindex` concurrently; assert exactly one effective writer (the other two emit `pipeline.lock_contention` and exit 0); zero double-indexing in `documents_vec`. *FR-RETRIEVAL-018, SC-RETRIEVAL-013, Constitution IX*

**Checkpoint Phase 6**: All four FR-RETRIEVAL-003 acceptance scenarios verified; all SP-005 lint surfaces green; constitutional grep-asserts pass.

---

## Phase 7: Polish

**Purpose**: Constitution re-evaluation, performance measurement, quickstart validation, root CLAUDE.md surface, final commit.

- [ ] T079 Re-evaluate Constitution Check against actual SP-005 code — for each of the 16 principles, run a fresh grep/AST scan of `packages/{index,inference,pipeline,storage,transport,cli,daemon,contracts}/src/**` SP-005-touched files; confirm all 16 remain `[x]` in `specs/005-retrieval/plan.md`; update plan.md Constitution Check section if any drift detected (zero expected per plan.md Complexity Tracking empty). *Constitution I-XVI, plan.md Constitution Check*
- [ ] T080 Empirically measure per-document four-sub-stage wall-clock on pai-node01 — run `tests/integration/end-to-end-retrieval.test.ts` against 10 mixed-MIME fixture documents under both `interactivePolicy` and `batchPolicy`; record p95 + mean wall-clock in plan.md "Performance Goals" footnote per Constitution XVI honesty. *FR-RETRIEVAL-022, SC-RETRIEVAL-021, Constitution XVI*
- [ ] T081 Empirically measure `corpus.find` p95 latency on pai-node01 — run against a 1000-doc indexed fixture corpus; record actual p95 in plan.md footnote; report honestly against §10.6 sub-20ms aspirational target and SP-005 honest sub-100ms commitment. *FR-RETRIEVAL-002, Performance Goals, Constitution XVI*
- [ ] T082 Walk `specs/005-retrieval/quickstart.md` end-to-end against actual SP-005 behavior — verify every step works as described against the user's pai-node01; correct any drift; mark "what's NOT working yet" partition (Tier 1/2/3 fallthrough → SP-006, kill-9 cross-stage → SP-006, retrieval-eval harness → v1.5+). *Constitution XVI*
- [ ] T083 Update `specs/005-retrieval/checklists/requirements.md` with implementation outcomes — for each of the 23 FR-RETRIEVAL-NNN and 22 SC-RETRIEVAL-NNN, mark verified/deferred/measured per actual code state. Document any FR/SC that landed differently than spec described (none expected). *spec.md Requirements, Success Criteria*
- [ ] T084 Add "SP-005 surface" section to root `CLAUDE.md` — mirror the "SP-004 surface" pattern with: (a) new telemetry classes (embed/index/edges/search), (b) the `corpus reindex` CLI command + dry-run mode, (c) degraded-signals behavior (FR-003 acceptance scenarios — partial signal failure produces `degraded_signals` annotation, not transport error), (d) confidence-weight config-map location (`config.toml [ranker.confidence_weights]`), (e) Tier 1/2/3 fallthrough is SP-006 scope (SP-005's `tier_used` is hardcoded `'hybrid'`). *Project documentation*
- [ ] T085 Final feature commit on branch `005-retrieval` — single squashed commit `feat(sp-005): hybrid retrieval — BM25 + dense + graph + confidence fusion, atomic index updates, cancellable corpus.find, corpus reindex CLI`; reference the spec/plan/research/data-model/contracts/tasks files; include "All 16 Constitution principles verified [x]" line. *Project commit convention*

**Checkpoint Phase 7**: Constitution Check 16/16 [x] re-verified; performance numbers measured + recorded honestly; quickstart walked end-to-end; CLAUDE.md surface documented; SP-005 ready for `/speckit-implement` hand-off to ProductBuild.

---

## Coverage Matrix — FR-RETRIEVAL-NNN

| FR | Covered by tasks |
|---|---|
| FR-RETRIEVAL-001 (corpus.find structured query → ranked SearchHit list) | T013, T031, T048, T055, T065, T068, T069 |
| FR-RETRIEVAL-002 (four-signal hybrid all-inputs) | T024, T025, T026, T027, T028, T031, T032, T037, T040, T042, T043, T044, T045, T047, T048, T074, T075 |
| FR-RETRIEVAL-003 (disabling any signal measurably changes top-K) | T031, T074, T075, T076, T077 |
| FR-RETRIEVAL-004 (structured error envelope as success MCP response) | T004, T013, T031, T040, T041, T055, T066, T075 |
| FR-RETRIEVAL-005 (vector ranking via cosine over per-doc embeddings) | T020, T023, T025, T033, T034, T043, T050 |
| FR-RETRIEVAL-006 (embed sub-stage post-classify, in drain-lock) | T020, T023, T033, T036, T037, T039, T050, T053, T056 |
| FR-RETRIEVAL-007 (atomic single-tx: FTS5+vec+edges+classify-UPDATE) | T019, T030, T034, T036, T038, T051, T053, T054, T071 |
| FR-RETRIEVAL-008 (edges materialization Jaccard + cosine + explicit_related) | T026, T029, T035, T044, T046, T052, T053 |
| FR-RETRIEVAL-009 (confidence weights config + post-fusion multiplier) | T010, T027, T028, T045, T047 |
| FR-RETRIEVAL-010 (Tier 1/2/3 deferred to SP-006 — `tier_used='hybrid'`) | T005, T014, T031, T048, T084 |
| FR-RETRIEVAL-011 (cancellable bounded IO; AbortSignal end-to-end) | T007, T016, T020, T023, T033, T035, T041, T048, T050, T052, T062 |
| FR-RETRIEVAL-012 (idempotency: reindex no-op on indexed corpus) | T057, T058, T059, T063 |
| FR-RETRIEVAL-013 (telemetry-or-die; ≥8 SP-005 event classes) | T005, T014, T020, T023, T031, T039, T040, T048, T072 |
| FR-RETRIEVAL-014 (library/CLI boundary — no process.exit in libs) | T006, T015, T063, T070 |
| FR-RETRIEVAL-015 (XDG paths via `Paths.*`) | T021, T070 |
| FR-RETRIEVAL-016 (zero subprocesses) | T021, T070 |
| FR-RETRIEVAL-017 (no new MCP mutation surfaces) | T012, T055 |
| FR-RETRIEVAL-018 (drain-lock single serialization point) | T036, T053, T056, T061, T063, T078 |
| FR-RETRIEVAL-019 (idempotent schema migration) | T008, T009, T017 |
| FR-RETRIEVAL-020 (Zod-validated SearchInput + SearchHit) | T002, T003, T013, T031, T048, T055, T066 |
| FR-RETRIEVAL-021 (frontmatter-field extraction at index-stage) | T024, T034, T042, T051 |
| FR-RETRIEVAL-022 (per-doc embed+index+edges budget 30s/120s) | T007, T016, T080 |
| FR-RETRIEVAL-023 (telemetry body-content prohibition; query SHA-256) | T005, T014, T048, T073 |

## Coverage Matrix — SC-RETRIEVAL-NNN

| SC | Covered by tasks |
|---|---|
| SC-RETRIEVAL-001 (end-to-end autonomous indexing — 4 MIME types) | T037, T056 |
| SC-RETRIEVAL-002 (Zod-validated SearchHit list from natural-lang query) | T031, T037, T048, T055 |
| SC-RETRIEVAL-003 (four-signal fusion materially different from single-signal) | T028, T077 |
| SC-RETRIEVAL-004 (dense-failure non-silent; degraded_signals annotation) | T032, T040, T075 |
| SC-RETRIEVAL-005 (ranking handles single-signal-match query) | T028, T076 |
| SC-RETRIEVAL-006 (`corpus reindex` backfill no missing/duplicate) | T057, T060, T063 |
| SC-RETRIEVAL-007 (`corpus reindex` clean exit on lock contention) | T061, T063 |
| SC-RETRIEVAL-008 (filter pushdown narrows candidate set before fusion) | T065, T067, T068, T069 |
| SC-RETRIEVAL-009 (unknown filter key → validation_error envelope) | T066, T055 |
| SC-RETRIEVAL-010 (telemetry event class coverage) | T005, T014, T072 |
| SC-RETRIEVAL-011 (atomic single transaction; rollback rolls back all 4 sub-stages) | T030, T038, T071 |
| SC-RETRIEVAL-012 (no partial index state after SIGKILL) | T030, T036, T038, T053, T071 |
| SC-RETRIEVAL-013 (drain-lock single-point serialization SP-003+SP-004+SP-005) | T061, T063, T078 |
| SC-RETRIEVAL-014 (cancellable IO under SIGTERM during search) | T041, T048 |
| SC-RETRIEVAL-015 (idempotent re-indexing — 0 calls on fully-indexed corpus) | T059, T063 |
| SC-RETRIEVAL-016 (no body content / no raw query in telemetry) | T014, T048, T073 |
| SC-RETRIEVAL-017 (XDG-paths-only lint passes) | T021, T070 |
| SC-RETRIEVAL-018 (library/CLI boundary lint passes) | T015, T021, T070 |
| SC-RETRIEVAL-019 (subprocess-hygiene lint passes) | T021, T070 |
| SC-RETRIEVAL-020 (Zod-validated SearchHit array shape; malformed → internal_error) | T003, T031, T048, T055 |
| SC-RETRIEVAL-021 (per-doc four-sub-stage wall-clock empirically measured) | T080 |
| SC-RETRIEVAL-022 (schema migration idempotency) | T008, T017 |

---

## Anti-claims (verified absent from this task list)

- **NO Tier 1/2/3 fallthrough tasks** — `tier_used` is hardcoded `'hybrid'` in SP-005; tier state machine deferred to SP-006 per FR-RETRIEVAL-010 + ARCHITECTURE-FINAL §10.6.
- **NO retrieval-eval harness tasks** (NDCG, MRR, recall@K) — explicitly out of scope per Constitution XVI / NFR-009; deferred to v1.5+.
- **NO non-Ollama embedding adapter tasks** — `nomic-embed-text` via Ollama HTTP is the only v1 path; future ADR for alternate adapters.
- **NO MCP mutation surface tasks** — SP-005 expands the existing `corpus.find` read-only tool handler; no new tools/resources/prompts introduced. Constitution III enforced.
- **NO tasks that bypass drain-lock** — every write path (daemon hook chain, `corpus reindex` CLI) acquires `Paths.drainLock()`. Single serialization point per Principle IX.
- **NO chunked-document embedding tasks** — single vector per document per ARCHITECTURE-FINAL §10.2.
- **NO HNSW/LSH/ANN tasks** — O(N²) edge materialization acceptable for N ≤ 10k per Plan-resolved bound.
- **NO worker-pool parallelism tasks** — single-threaded per-doc embed+index+edges-build mirroring SP-004 FR-CLASSIFY-019.
- **NO `corpus reindex --since <date>` subset tasks** — v1 surface is whole-corpus reindex only.
- **NO cross-encoder re-ranking tasks** — RRF fusion only; future horizon if quality data demands.
- **NO embedding-cache tasks** — embeddings live in `documents_vec` only.
