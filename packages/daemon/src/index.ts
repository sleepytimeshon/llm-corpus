// @llm-corpus/daemon — worker-thread egress-guard registration shim.
// Real implementation (worker-bootstrap.ts, worker-spawn-guard.ts) is
// scoped to T049 / T050 (Phase 4). SP-001 ships this placeholder so the
// package has source content for tsc.
export {};
