node_modules/
*.db
*.db-wal
*.db-shm
.telemetry*.jsonl
.source-index.jsonl
.tmp-*
*.tmp

# --- Build outputs (T009) ---
dist/
coverage/
*.tsbuildinfo

# --- Claude Code ---
# spec-kit installs slash commands here; treated as derived state
# (re-materialized by `specify init --here`). May also hold auth tokens.
.claude/
